apiVersion: v1
kind: Service
metadata:
  name: django-service
spec:
  type: NodePort
  selector:
    app: django-app
  ports:
    - protocol: TCP
      port: 8000 # Internal Django port
      targetPort: 8000 # Container port
      nodePort: 30007 # Externally accessible port on Minikube node
